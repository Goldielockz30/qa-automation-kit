# === Core App Vars ===
APP_ENV=development
APP_PORT=8000
BASE_URL=http://localhost:8000
COVERAGE_MIN=70

# Headless mode (false = show browser by default, true = hide it for CI/speed)
HEADLESS=true
BROWSER=chromium

# === Flexible E2E Demo ===
# If not set, defaults to the bundled demo page (backend/tests/e2e/pages/login.html)
# Change this to test a real app login (e.g. https://www.facebook.com/login)
# E2E_URL=https://www.facebook.com/login

# Credentials for demo or real login page
LOGIN_USERNAME=user
LOGIN_PASSWORD=pass

# Selectors (defaults match bundled login.html)
USER_SELECTOR=#username
PASS_SELECTOR=#password
SUBMIT_SELECTOR=#login

# If true: only open page and pause (no autofill/click)
DEMO_PAUSE_ONLY=true
